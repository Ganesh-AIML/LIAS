JWT_SECRET_KEY=replace_with_a_strong_random_secret

# MongoDB Atlas (production datastore)
# Append ?retryWrites=true&w=majority to the URI for production resilience
MONGO_URI=mongodb+srv://user:password@cluster.mongodb.net/lias?retryWrites=true&w=majority
MONGO_DB_NAME=lias

# Admin bootstrap: seeds first admin on startup (only while zero admins exist).
# Until seeded, ADMIN_SECRET (X-Admin-Token header) stays valid as a fallback.
ADMIN_SECRET=replace_with_64_char_hex_secret
ADMIN_EMAIL=replace_with_admin_email
ADMIN_PASSWORD=replace_with_a_strong_admin_password

DB_ENCRYPTION_KEY=replace_with_a_fernet_compatible_key
ALLOWED_ORIGINS=https://your-frontend.onrender.com,http://localhost:5173
JWT_EXPIRY_SECONDS=7200
FRONTEND_BASE_URL=https://yoursite.com
